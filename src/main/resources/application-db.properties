spring.datasource.url = jdbc:postgresql://${PGHOST}:${PGPORT}/${PGNAME}
jakarta.persistence.jdbc.url = jdbc:postgresql://${PGHOST}:${PGPORT}/${PGNAME} 
spring.datasource.username = ${PGUSER}
spring.datasource.password = ${PGPASSWORD}

spring.jpa.hibernate.ddl-auto = update
